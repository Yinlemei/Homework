#def collatz(number):
  if number%2==0:
     print(str(number//2))
        return number//2
  elif number%2==1:
     print(str(3*n+1))
        return 3*n+1
  print(`请输入一个整数：`)
    number=int(input（））
    while number!=1:
       number=int(collatz(int(number)))
    if number==1:
       break